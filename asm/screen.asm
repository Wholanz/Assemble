DATA SEGMENT
	STR DB 'INPUT STRING IN THE WINDOW!','$'
DATA ENDS

STACK SEGMENT

STACK ENDS

CODE SEGMENT
	ASSUME CS:CODE, DS:DATA
SCREEN:
	MOV AX, DATA
	MOV DS, AX
	
	MOV AH,06H
	MOV AL,0
	MOV BH,01110000B
	MOV CH,4
	MOV CL,9
	MOV DH,19
	MOV DL,69
	INT 10H
	
	MOV AH, 01H
	MOV CH, 0
	MOV CL, 19
	INT 10H
	
	MOV AH, 02H
	MOV BH, 00H
	MOV DH, 12
	MOV DL, 26
	INT 10H
	
	LEA DX, STR
	MOV AH, 09H
	INT 21H
	
	MOV AH, 4CH
	;INT 21H
	
CODE ENDS

END SCREEN